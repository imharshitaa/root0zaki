baseURL: "https://imharshitaa.github.io/root0zaki/"
languageCode: en-us
title: "root0zaki"
theme: PolyRhythmic

# Add custom parameters for theme
params:
  logo: "/images/root.jpg"            # 1. Logo path (put file in static/images/logo.png)
  homeURL: "/"                        # 2. Make site title/logo clickable → homepage
  disableThemeToggle: true            # 3. Removes the weird "palette" icon
  googlefonts: true                   # 4. Ensures fonts load correctly

menu:
  main:
    - name: "About"
      pageRef: "/about"
      weight: 10
    - name: "Projects"
      pageRef: "/projects"
      weight: 20
    - name: "Research"
      pageRef: "/research"
      weight: 30
    - name: "Docs"
      pageRef: "/docs"
      weight: 40
    - name: "Pentesting"
      pageRef: "/pentesting"
      weight: 50
