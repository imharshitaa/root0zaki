baseURL: "https://imharshitaa.github.io/root0zaki/"
languageCode: en-us
title: "root0zaki"
theme: PolyRhythmic

# Add custom parameters for theme
params:
  logo: "/images/logo.png"            # 1. Logo path (put file in static/images/logo.png)
  homeURL: "/"                        # 2. Make site title/logo clickable → homepage           
  googlefonts: true                   # 4. Ensures fonts load correctly
  disableThemeToggle: true
  customCSS:
    - "css/custom.css"


menu:
  main:
    - name: "About"
      pageRef: "/about"
      weight: 10
    - name: "Projects"
      pageRef: "/projects"
      weight: 20
    - name: "Research"
      pageRef: "/research"
      weight: 30
    - name: "Docs"
      pageRef: "/docs"
      weight: 40
    - name: "Pentesting"
      pageRef: "/pentesting"
      weight: 50
